



describe("History Api Fallback", () =>{
    it("Should fallback if requested by accept html", async () =>{

    })

    it("Should not fallback if requested by accept JSON", async () => {

    })

    it("Should not fallback if request already have handler (controller)", async () => {
        
    })

    it("Should not fallback if request already serve static file", async () => {
        
    })

    it("Should return 404 if requested non exists file", async () => {

    })

    it("Should throw error if multiple @route.historyApiFallback() defined", async () => {

    })

    it("Should throw error if @route.historyApiFallback() combined with non GET method", () => {

    })
})