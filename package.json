{
	"private": true,
	"description": "Pleasant TypeScript Web Api Framework",
	"scripts": {
		"test": "jest",
		"lint": "tslint -c ./tslint.json --project ./tsconfig.json --format stylish",
		"clean": "rimraf packages/**/lib && lerna run package-dev",
		"compile": "npm run clean && jest && lerna run compile && lerna run package-prod",
		"canary": "npm run compile && lerna publish -c --exact && npm run clean"
	},
	"author": "Ketut Sandiarsa",
	"devDependencies": {
		"@types/jest": "^23.1.0",
		"coveralls": "^3.0.1",
		"edit-json-file": "^1.0.8",
		"jest": "^23.1.0",
		"lerna": "^2.11.0",
		"pre-push": "^0.1.1",
		"reflect-metadata": "^0.1.12",
		"rimraf": "^2.6.2",
		"ts-jest": "^23.0.0",
		"tslint": "^5.10.0",
		"typescript": "^2.9.2",
		"typestrict": "^0.0.9"
	},
	"workspaces": [
		"packages/*"
	],
	"pre-push": [
		"test"
	]
}
